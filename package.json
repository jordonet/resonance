{
  "name": "deepcrate-monorepo",
  "private": true,
  "scripts": {
    "deps:check": "pnpm list -r",
    "deps:update": "pnpm -r up --latest --interactive",
    "dev": "pnpm --filter server --filter ui dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "esbuild",
      "sqlite3"
    ],
    "overrides": {
      "qs": ">=6.14.2",
      "tar": ">=7.5.7"
    }
  }
}
